{
  "name": "mrc-demo-api",
  "version": "1.0.0",
  "description": "Mock API for MRC Roussillon Waste Management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}